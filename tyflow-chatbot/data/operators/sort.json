{
  "url": "https://docs.tyflow.com/tyflow_particles/operators/sort",
  "title": "Sortoperator",
  "category": "Operators",
  "operator": "Sortoperator",
  "sections": [
    {
      "heading": "Sortoperator",
      "level": "h2",
      "anchor": "sort-operator",
      "content": "The Sort operator allows you to sort particles by their custom properties.\n\nSorting particles is useful when you want to control the order in which they are processed by other operators. For example, if you have a stack of particles that are unordered, and you want to process them by another operator from top-to-bottom, you could assign their “position Z” value to a custom float channel using a Custom Properties operator, then use a Sort operator to sort their event particle order using that “position Z” value as the sort value, in descending order. Then you could use another operator (ex: Split) to process them in the desired order. Note: the Sort operator is meant for sortinginternalparticle properties (like ID or event order), notphysicalproperties (like position in space) - you can sort ID or event order using physical propertiesas the sort criteria(as explained in the prior example), but the Sort operator isn’t meant to be used to modify physical properties directly.",
      "params": []
    },
    {
      "heading": "Sort mode",
      "level": "h4",
      "anchor": "sort-mode",
      "content": "Sort particles by IDs: sorts particle IDs by re-assigning IDs based on their ordered sort value.\n\nBecause “sort particles by IDs” potentially re-assigns particle IDs, this can impact any previously-assigned ID-dependent properties (ex: particle bindings). Make sure you sort by IDs prior to assigning ID-dependent properties in the flow.\n\nSort event particle order: sorts particles’ explicit ordering within the event based on their ordered sort value.",
      "params": [
        "Sort particles by IDs",
        "Sort event particle order"
      ]
    },
    {
      "heading": "Sort value",
      "level": "h4",
      "anchor": "sort-value",
      "content": "Channel: the custom float channel from which each particle’s sortable value will be derived.",
      "params": [
        "Channel"
      ]
    },
    {
      "heading": "Stacking",
      "level": "h4",
      "anchor": "stacking",
      "content": "Stack sorted particles: when enabled, sorted particles will be stacked (ordered by location in space) along the specified axis.Consider particle meshes: when enabled, the bounding box of particle meshes will be taken into consideration when stacking particles, to avoid overlap.Flip stack: when enabled, the direction of the stack along the specified axis will be flipped.Separation: the minimum distance between stacked particles.Variation %: the per-particle percentage of variation to apply.\n\nStack sorted particles: when enabled, sorted particles will be stacked (ordered by location in space) along the specified axis.\n\nConsider particle meshes: when enabled, the bounding box of particle meshes will be taken into consideration when stacking particles, to avoid overlap.\n\nFlip stack: when enabled, the direction of the stack along the specified axis will be flipped.\n\nSeparation: the minimum distance between stacked particles.",
      "params": [
        "Consider particle meshes",
        "Separation",
        "Flip stack",
        "Stack sorted particles"
      ]
    },
    {
      "heading": "Clustering",
      "level": "h4",
      "anchor": "clustering",
      "content": "Enable clustering: when enabled, particles will only be sorted with other particles that have the same cluster value.\n\nEnabling clustering allows you to sort groups of particles independently from each other.",
      "params": [
        "Enable clustering"
      ]
    }
  ],
  "crawled_at": "2025-11-11T11:52:04Z"
}